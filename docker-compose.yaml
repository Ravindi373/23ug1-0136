version: "3.8"

services:
  web:
    build: ./web
    ports:
      - "5000:5000"
    environment:
      DB_HOST: db
<<<<<<< HEAD
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${DB_NAME}
=======
      DB_USER: root
      DB_PASSWORD: example
      DB_NAME: mydb
>>>>>>> 3128f28929f2f1aa6f8513f75c59d9744521211b
    depends_on:
      - db
    networks:
      - app-net
    restart: on-failure

  db:
    image: mysql:8.0
    environment:
<<<<<<< HEAD
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    # No host port mapping - only the web container needs to reach MySQL,
    # and it does so over the internal app-net network.
=======
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: mydb
    ports:
      - "3306:3306"
>>>>>>> 3128f28929f2f1aa6f8513f75c59d9744521211b
    volumes:
      - db-data:/var/lib/mysql
    networks:
      - app-net
    restart: on-failure

volumes:
  db-data:
    external: true

networks:
  app-net:
    external: true
